const axios = require("axios");

const readTwittests = (to, subject, json, cc = "", attachments = null) => {
    try {

        // axios.get('https://api.twitter.com/2/tweets/search/recent?query=mahsaamini', config)
        //     method: 'get',
        //     url: ,
        //     headers: {
        //         'Authorization': 'Bearer AAAAAAAAAAAAAAAAAAAAA',
        //         'Cookie': 'guest_id=v1%3A166415062264839583'
        //     }
        // };
    }
    catch (ex) { /* empty */ }
}

module.exports = apiTwitter