const axios = require("axios");

const readTwittests = (to, subject, json, cc = "", attachments = null) => {
    try { /* empty */ }
    catch (ex) { /* empty */ }
}

module.exports = {
    readTwittests,
}